from __future__ import annotations

from pathlib import Path

def launch(review_file: Path) -> None:  # placeholder stub
    print(f"TUI would open {review_file}. (Install with extras: `pip install cloak-privacy[tui]`)")
